source "http://rubygems.org"
gemspec

group :test do
  gem 'wrong' # nicer assertions
  gem 'turn' # a better test runner
  gem 'ansi' # so turn can do pretty colours
  gem 'sqlite3'
  gem 'rails', '> 3.2.0'
end
